---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart adguardhome
  ansible.builtin.systemd:
    name: adguardhome
    state: restarted
    daemon_reload: true
